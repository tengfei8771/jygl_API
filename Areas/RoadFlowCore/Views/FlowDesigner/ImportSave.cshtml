<script type="text/javascript">
    var errmsg = "@Html.Raw(ViewData["errmsg"])";
    if (errmsg.length > 0) {
        alert(errmsg);
        window.location = "Import@(Html.Raw(ViewData["queryString"]))";
    } else {
        alert("导入成功!");
        parent.query();
        new RoadUI.Window().close();
    }
</script>